import { FavouriteData } from './favouriteSchema';

const schemaVersion = 21;

const combinedSchema = [FavouriteData];

export const SchemaVersion = schemaVersion;
export const CombinedSchema = combinedSchema;
